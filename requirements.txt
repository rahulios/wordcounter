# Core runtime (Windows-only writing tracker)
pynput>=1.7.6            # global keyboard listener (gated by focus + allowlist)
pandas>=2.0.0            # data handling + Excel I/O
openpyxl>=3.1.0          # .xlsx engine used by pandas
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
psutil>=5.9.0
pywin32>=306             # win32gui / win32process / SetWinEventHook (Windows only)

# --- Dev / packaging (optional, uncomment when needed) ---
pyinstaller>=6.0.0        # build single-file .exe via wordcounter.spec
# pytest>=7.4.0
